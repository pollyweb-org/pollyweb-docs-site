{
  "name": "pollyweb-docs-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "security:check": "node scripts/check-security-issues.mjs",
    "hooks:install": "git config core.hooksPath .githooks",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "playwright": "^1.58.2",
    "vitest": "^3.2.4"
  }
}
