version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - echo "No build step for static site"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
